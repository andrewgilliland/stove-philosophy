---
import "../styles/global.css";
import { BookOpen, Flame, House, Info, Soup } from "@lucide/astro";

const {
  title = "Stove Philosophy",
  description = "Stove Philosophy: the ancient art of enduring heat, waiting patiently, and accepting that some things just need to simmer.",
  image = "/images/og-stove-philosophy.svg",
} = Astro.props;
const pathname = Astro.url.pathname;
const canonicalUrl = Astro.url;
const imageUrl = new URL(image, Astro.url);

const navItems = [
  { href: "/", label: "Home", Icon: House },
  { href: "/about", label: "About", Icon: Info },
  { href: "/principles", label: "Core Principles", Icon: BookOpen },
  { href: "/burners", label: "Four Burners", Icon: Flame },
  { href: "/leftovers", label: "Leftovers", Icon: Soup },
];

const navLinkClass = (href: string) =>
  `inline-flex items-center gap-2 rounded-md px-3 py-2 text-sm transition ${
    pathname === href
      ? "bg-indigo-500/20 text-indigo-200 ring-1 ring-indigo-400/40"
      : "text-slate-300 hover:bg-cyan-500/10 hover:text-cyan-200"
  }`;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="generator" content={Astro.generator} />
    <link rel="canonical" href={canonicalUrl} />
    <meta name="description" content={description} />

    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:image" content={imageUrl} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={imageUrl} />
    <title>{title}</title>
  </head>
  <body
    class="min-h-screen overflow-x-hidden bg-slate-950 text-slate-100 antialiased"
  >
    <div
      aria-hidden="true"
      class="ambient-glow pointer-events-none fixed inset-x-0 top-0 -z-10 h-72"
    >
    </div>
    <header
      class="border-b border-slate-800 bg-slate-950/90 shadow-[0_0_40px_rgba(56,189,248,0.08)] backdrop-blur"
    >
      <div
        class="mx-auto flex max-w-5xl flex-wrap items-center justify-between gap-3 px-6 py-4"
      >
        <a
          href="/"
          class="brand-shimmer bg-linear-to-r from-amber-300 via-orange-300 to-rose-300 bg-clip-text text-sm font-semibold tracking-wide text-transparent"
          >Stove Philosophy</a
        >
        <nav class="flex flex-wrap gap-1">
          {
            navItems.map((item) => (
              <a href={item.href} class={navLinkClass(item.href)}>
                <item.Icon
                  size={16}
                  stroke-width={2}
                  class="h-4 w-4"
                  aria-hidden="true"
                />
                {item.label}
              </a>
            ))
          }
        </nav>
      </div>
      <div
        class="gradient-sweep h-px bg-linear-to-r from-transparent via-cyan-400/60 to-transparent"
      >
      </div>
    </header>
    <slot />
  </body>
</html>

<style>
  html,
  body {
    margin: 0;
    width: 100%;
    height: 100%;
  }

  .ambient-glow {
    background:
      radial-gradient(
        600px 220px at 15% -10%,
        rgba(99, 102, 241, 0.22),
        transparent 70%
      ),
      radial-gradient(
        500px 220px at 85% 0%,
        rgba(34, 211, 238, 0.18),
        transparent 72%
      ),
      radial-gradient(
        460px 220px at 50% -20%,
        rgba(245, 158, 11, 0.14),
        transparent 75%
      );
    filter: blur(34px);
    animation: ambientPulse 12s ease-in-out infinite;
  }

  .brand-shimmer {
    background-size: 200% 100%;
    animation: brandShimmer 10s linear infinite;
  }

  .gradient-sweep {
    background-size: 180% 100%;
    animation: lineSweep 9s ease-in-out infinite;
  }

  @keyframes ambientPulse {
    0%,
    100% {
      opacity: 0.38;
      transform: translateY(0px) scale(1);
    }
    50% {
      opacity: 0.52;
      transform: translateY(-4px) scale(1.02);
    }
  }

  @keyframes brandShimmer {
    0% {
      background-position: 0% 50%;
    }
    100% {
      background-position: 200% 50%;
    }
  }

  @keyframes lineSweep {
    0%,
    100% {
      background-position: 0% 50%;
      opacity: 0.75;
    }
    50% {
      background-position: 100% 50%;
      opacity: 1;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .ambient-glow,
    .brand-shimmer,
    .gradient-sweep {
      animation: none;
    }
  }
</style>
